const reducer = (state = null, action = {}) => {
  switch (action.type) {
  case 'SELECT_USER':
    return

    action.payload

    // action.payload
  default:
    return state
  }
}

export default reducer
